// SPDX-License-Identifier: MIT
pragma solidity >=0.4.22 <0.8.0;

// Define a contract 'ConsumerRole' to manage this role - add, remove, check
contract  ConsumerRole  {//abstract contract  ConsumerRole  {

  // Define a modifier that checks to see if msg.sender has the appropriate role
  //modifier onlyConsumer() virtual;

//   // Define a function 'isConsumer' to check this role
//   function isConsumer(address account) public virtual view returns (bool);

//   // Define a function 'addConsumer' that adds this role
//   function addConsumer(address account) public virtual;

//   // Define a function 'renounceConsumer' to renounce this role
//   function renounceConsumer() public virtual;

}
